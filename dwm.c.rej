--- dwm.c
+++ dwm.c
@@ -805,6 +893,9 @@ drawbar(Monitor *m) {
 	if(m == selmon) { /* status is only drawn on selected monitor */
 		w = TEXTW(stext);
 		x = m->ww - w;
+		if(showsystray && m == systraytomon(m)) {
+			x -= getsystraywidth();
+		}
 		if(x < xx) {
 			x = xx;
 			w = m->ww - xx;
